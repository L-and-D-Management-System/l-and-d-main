fullnameOverride: user-service
replicaCount: 1
image:
  repository: landd/user-service
  pullPolicy: IfNotPresent
  tag: "latest"

service:
  type: ClusterIP
  port: 3000

env:
  MONGODB_URI: "mongodb://mongodb:27017/user-db"
  PORT: "3000"

secrets:
  JWT_SECRET: "your-jwt-secret-here"

