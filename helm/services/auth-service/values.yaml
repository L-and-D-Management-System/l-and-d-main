fullnameOverride: auth-service
replicaCount: 1
image:
  repository: landd/auth-service
  pullPolicy: IfNotPresent
  tag: "latest"

service:
  type: ClusterIP
  port: 3000

env:
  MONGO_URI: "mongodb://mongodb:27017/auth-db"
  PORT: "3000"

secrets:
  JWT_SECRET: "your-jwt-secret-here"

